# Pages Demo Branch Content

This folder is designed to be mirrored to a `pages` branch.

## Publish process

1. Create/update `pages` branch from repository root.
2. Ensure `pages/site` is present.
3. Push changes; workflow in `pages/site/.github/workflows/pages.yml` deploys demo site.
